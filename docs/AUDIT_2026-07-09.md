# Audit 2026-07-09

Scope: live site `https://477477.ru`, host `dev477477@100.66.106.32`, project path `/opt/plumbing_store_v2/new-store-v2`.

## Confirmed live fixes

1. Open WebUI on port `8443` was restored to the previous public listener state by operator request.
   - Current: nginx listens on public `0.0.0.0:8443` / `[::]:8443`.
   - Verified: `https://477477.ru:8443/` returns `200`.

2. Disabled nginx version disclosure.
   - `server_tokens off;` is enabled in `/etc/nginx/nginx.conf`.
   - Public responses now return `server: nginx` without an exposed nginx version.

3. Fixed synthetic monitoring false positives in anti-bot checks.
   - Requests from `Mozilla/5.0 pumbum-monitoring/1.0` were hitting `429` on catalog pages.
   - nginx now supplies browser-like upstream `User-Agent` and `Accept-Language` headers for `HEAD` probes and the known monitoring user agent.
   - Verified `GET /catalog`, `GET /search?q=Valtec`, and `HEAD /catalog/nasosy-i-vodosnabzhenie` return `200` for the monitor profile.
   - Script clients such as `python-requests/2.31` are still blocked on protected catalog pages.

4. Fixed disabled order APIs at the live edge.
   - `POST /api/cart` returns `410` JSON with `reason: disabled`.
   - `POST /api/leads` returns `410` JSON with `reason: disabled`.
   - This matches the documented state while the app container rebuild is blocked.

## Source changes prepared

These changes are in source and must be included in the next safe app rebuild:

1. `src/middleware.ts`
   - Treats `/api/cart` and `/api/leads` as documented disabled APIs instead of generic write APIs.
   - Allows documented probes for `HEAD` requests.
   - Treats `pumbum-monitoring/1.0` as a trusted synthetic monitor.

2. `src/lib/catalog/route-map.ts`
   - Removed `/search` from `sitemap.xml` route generation.
   - Reason: `/search` is `noindex,nofollow`, and `docs/SEO_RULES.md` says technical/search pages must not be included in sitemap.

## Major blocker

Do not rebuild or redeploy the app container until the live catalog source is restored.

Confirmed mismatch:

- Live `/api/health`: `products=9208`, `published=9208`, `categories=6`.
- Source file `/opt/plumbing_store_v2/new-store-v2/content/generated/legacy-catalog.json`: `5700` products.
- The running container does not expose `/app/content/generated/legacy-catalog.json`; catalog data appears bundled into the current `.next` build/image.

Risk:

- A normal `docker compose up --build` from the current source can roll the live catalog back from `9208` products to `5700` products.

Required before rebuild:

1. Restore or regenerate the current `9208`-product generated catalog source.
2. Verify generated manifests and image assets match the live build.
3. Run the normal validation/build flow only after the source/runtime catalog counts match.

## Remaining findings

1. Live sitemap still includes `/search` until the app is safely rebuilt or sitemap is otherwise regenerated from corrected source.
2. Project docs still reference the older `5700`-product catalog in several places; this should be updated only after the correct `9208` source artifact is found.
3. Docker container hardening is partially good but not complete.
   - Confirmed good: app binds to `127.0.0.1:3010`, runs as `node`, has `no-new-privileges`, memory/CPU/PID limits, and product images mounted read-only.
   - Remaining gaps: no read-only root filesystem and no explicit `cap_drop`.
4. Host has no `ufw` installed. Current exposure is controlled by nginx bindings and service bindings; provider-level firewall was not confirmed.
5. Security headers are present, including HSTS and CSP, but CSP still allows inline scripts/styles. This may be required by the current Next/Yandex setup and needs careful testing before tightening.
6. Some security headers are duplicated between app and nginx. This is low severity but should be cleaned up during the next config pass.

## Verification performed

- Public pages checked with browser-like headers:
  - `/`
  - `/catalog`
  - `/search?q=Valtec`
  - `/catalog/nasosy-i-vodosnabzhenie/aquario-2436`
  - `/contacts`
- Sampled sitemap routes returned `200`.
- `npm audit --omit=dev --audit-level=moderate --package-lock-only` reported no production dependency vulnerabilities.
- App logs were clean after the nginx changes.
- nginx config passed `nginx -t` before reload/restart.

## Backups

Server-side backups were created under:

- `/opt/plumbing_store_v2/deploy-backups/audit-20260709-20260709T011224Z`
- `/opt/plumbing_store_v2/deploy-backups/audit-20260709-20260709T011705Z`
- `/opt/plumbing_store_v2/deploy-backups/audit-20260709-20260709T011800Z`
- `/opt/plumbing_store_v2/deploy-backups/audit-20260709-20260709T011923Z`
- `/opt/plumbing_store_v2/deploy-backups/audit-20260709-source-final`
- `/opt/plumbing_store_v2/deploy-backups/audit-20260709-revert-8443-current.conf`
